// DEPRECATED: usar @/app/lib/auth/permissions en su lugar.
// Este archivo se mantiene para evitar romper imports existentes,
// pero re-exporta desde la version canonica.
export { getNavPermissions } from "@/app/lib/auth/permissions";
export type { NavPerms } from "@/app/lib/auth/permissions";
